[package]
name = "mal_linear"
version = "0.1.0"
edition = "2021"
description = "MAL Linear Logic — Resource-aware reasoning via ⊗, &, ⊕, ⅋, !"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
mal_dependent = { path = "../dependent" }
