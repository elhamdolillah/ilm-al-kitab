[package]
name = "mal_ownership"
version = "0.1.0"
edition = "2021"
description = "MAL Ownership Checker: Linear logic (⊸) enforcement for memory safety"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_types = { path = "../types" }
