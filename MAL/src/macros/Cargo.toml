[package]
name = "mal_macros"
version = "0.1.0"
edition = "2021"
description = "MAL Hygienic Macros — Lambda calculus with hygiene enforcement"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
