[package]
name = "mal_async"
version = "0.1.0"
edition = "2021"
description = "MAL Async — Effect system via std::future + simple executor"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
mal_traits = { path = "../traits" }
mal_monadic = { path = "../monadic" }
