[package]
name = "mal_monadic"
version = "0.1.0"
edition = "2021"
description = "MAL Monadic types — Result + Option + Monad trait"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
mal_traits = { path = "../traits" }
