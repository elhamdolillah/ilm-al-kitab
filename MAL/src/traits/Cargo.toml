[package]
name = "mal_traits"
version = "0.1.0"
edition = "2021"
description = "MAL Traits/Typeclasses — ad-hoc polymorphism via dictionary passing"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
