[package]
name = "mal_adt"
version = "0.1.0"
edition = "2021"
description = "MAL Algebraic Data Types — Sum + Product types"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
