[package]
name = "mal_model"
version = "0.1.0"
edition = "2021"
description = "MAL Model Theory — First-order logic, structures, satisfaction"
license = "MIT"
[lib]
path = "src/lib.rs"
crate-type = ["lib"]
[dependencies]
mal_arena = { path = "../arena" }
mal_index = { path = "../index" }
