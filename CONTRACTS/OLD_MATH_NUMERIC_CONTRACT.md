# OLD-MATH-NUMERIC-001 — عقد الدقة العددية القديم

## الحالة

`RESEARCH` — لا يثبت توافق MAL الجديد.

## المصدر

الأرشيف المرفق `arabic-math-lang-main(2).zip`, خصوصاً `tests/c40_q32_verification/`.

## ما يثبته المصدر القديم فقط

- اختبار Python للضرب المختلط: 265/265.
- اختبار Assembly مقابل مرجع C `__int128`: 10016/10016.
- إعادة إنتاج C40 الأساسية: 8/8.
- اختبار الإجهاد القديم: 17/21، ولذلك لا يجوز إعلان الدقة الكاملة.

## عقد التكامل

لا يُنقل `أُس()` إلى MAL الجديد إلا بعد تحديد التمثيل العددي، rounding، overflow، underflow، zero division، وABI؛ ثم corpus مستقل يقارن المرجعين ويثبت stdout وexit code وSHA-256. غياب أي بند يبقي الحالة `DEFERRED_UNTIL_NEEDED`.

## شرط عدم التلوث

لا تعديل Parser/Arena/Compiler في هذه المرحلة. التنفيذ التجريبي، إن لزم، يبقى في `RESEARCH/` أو فرع معزول.
